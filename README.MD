# DockerSample

This repository contains a sample project for demonstrating how to use Docker with a simple Blazor application. The project includes a Dockerfile for building the Docker image and a docker-compose.yml file for running the application in a containerized environment.